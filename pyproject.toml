[project]
name = "gsirdf-clone"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "curl-cffi>=0.15.0",
    "requests>=2.33.1",
]
